#include <stdio.h>
#include <stdlib.h>

typedef struct point
{
  int x;
  int y;
} point;

typedef struct size
{
  int width;
  int height;
} size;

typedef struct rectangle
{
  point.x = 1;
  point.y = 4;
  size.width = 40;
  size.height = 20;
} rect;

int main(){

  printf("The base of the rectangle is at x: %i, y: %i with dimensions w: %i, h: %i", rect.point.x, rect.point.y, rect.size.width, rect.size.height);

  return 0;
}
